# =============================================================================
# Darkenyas CRM - Environment Variables
# =============================================================================
# 1. Copy: cp .env.example .env
# 2. Fill SECRET_KEY, DB_* (PostgreSQL). Generate key: python -c "import secrets; print(secrets.token_urlsafe(50))"
# 3. Create DB: createdb djcrm  (or via pgAdmin)
# 4. Run: python manage.py migrate && python manage.py runserver
#
# For SQLite instead: comment out DB_ENGINE and DB_* below.
# For Render: use DATABASE_URL in Dashboard (see bottom).
# =============================================================================

SECRET_KEY=your-secret-key-here
DEBUG=True

# -----------------------------------------------------------------------------
# Email - Required for signup verification & password reset
# -----------------------------------------------------------------------------
# Option A: Gmail API (for Render - SMTP ports 25/465/587 are blocked)
# Set USE_GMAIL_API=true and configure below. Get refresh token via:
#   gmail_oauth2 --generate_oauth2_token --client_id="..." --client_secret="..." --scope="https://www.googleapis.com/auth/gmail.send"
# -----------------------------------------------------------------------------
USE_GMAIL_API=false
GMAIL_API_CLIENT_ID=your-client-id.apps.googleusercontent.com
GMAIL_API_CLIENT_SECRET=your-client-secret
GMAIL_API_REFRESH_TOKEN=1//0xxxxxxxxxxxx
DEFAULT_FROM_EMAIL=your-email@gmail.com

# Option B: SMTP (for local dev - Gmail App Password)
# Use when USE_GMAIL_API is false. Gmail: Enable 2FA, create App Password
# -----------------------------------------------------------------------------
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password-here

# -----------------------------------------------------------------------------
# Database - PostgreSQL (default, matches production)
# -----------------------------------------------------------------------------
DB_ENGINE=django.db.backends.postgresql
DB_NAME=djcrm
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432
DB_SSL=false

# Render/Neon: use DATABASE_URL in Dashboard instead of DB_*
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# -----------------------------------------------------------------------------
# Render.com deploy - Add in Render Dashboard (not in .env)
# -----------------------------------------------------------------------------
# Required: DATABASE_URL, SECRET_KEY, DEBUG=False
# Email (Gmail API - Render blocks SMTP): USE_GMAIL_API=true, GMAIL_API_CLIENT_ID, GMAIL_API_CLIENT_SECRET, GMAIL_API_REFRESH_TOKEN, DEFAULT_FROM_EMAIL
# Optional: DJANGO_SUPERUSER_EMAIL, DJANGO_SUPERUSER_USERNAME, DJANGO_SUPERUSER_PASSWORD
# Custom domain: ALLOWED_HOSTS, CSRF_TRUSTED_ORIGINS, SITE_URL
