# =============================================================================
# Darkenyas CRM - Environment Variables
# Copy this file to .env and fill in your values
# =============================================================================

SECRET_KEY=your-secret-key-here
DEBUG=True

# -----------------------------------------------------------------------------
# Email (SMTP) - Required for signup verification & password reset
# -----------------------------------------------------------------------------
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password-here
DEFAULT_FROM_EMAIL=noreply@djcrm.com

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# Option A: DATABASE_URL (Render, Neon, etc.) - takes precedence
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Option B: Individual vars (local PostgreSQL)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=djcrm
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432
DB_SSL=false

# -----------------------------------------------------------------------------
# Production - Render.com
# Add manually in Render Dashboard: DATABASE_URL, SECRET_KEY, DEBUG, EMAIL_HOST_*,
# DEFAULT_FROM_EMAIL, DJANGO_SUPERUSER_*
#
# Using *.onrender.com: RENDER_EXTERNAL_HOSTNAME is set automatically, no extra
# setup needed.
#
# Using a custom domain (e.g. crm.example.com), add these to Render:
# ALLOWED_HOSTS=crm.example.com
# CSRF_TRUSTED_ORIGINS=https://crm.example.com
# SITE_URL=https://crm.example.com
# -----------------------------------------------------------------------------

# First admin user (created on deploy via build.sh createsuperuser)
# DJANGO_SUPERUSER_EMAIL=admin@example.com
# DJANGO_SUPERUSER_USERNAME=admin
# DJANGO_SUPERUSER_PASSWORD=your-secure-password
