# =============================================================================
# Darkenyas CRM - Environment Variables
# =============================================================================
# 1. Copy: cp .env.example .env
# 2. Fill SECRET_KEY, DB_* (PostgreSQL). Generate key: python -c "import secrets; print(secrets.token_urlsafe(50))"
# 3. Create DB: createdb djcrm  (or via pgAdmin)
# 4. Run: python manage.py migrate && python manage.py runserver
#
# For SQLite instead: comment out DB_ENGINE and DB_* below.
# For Render: use DATABASE_URL in Dashboard (see bottom).
# =============================================================================

SECRET_KEY=your-secret-key-here
DEBUG=True

# -----------------------------------------------------------------------------
# Email (SMTP) - Required for signup verification & password reset
# Gmail: Enable 2FA, create App Password → Security → App passwords
# -----------------------------------------------------------------------------
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password-here
DEFAULT_FROM_EMAIL=noreply@djcrm.com

# -----------------------------------------------------------------------------
# Database - PostgreSQL (default, matches production)
# -----------------------------------------------------------------------------
DB_ENGINE=django.db.backends.postgresql
DB_NAME=djcrm
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432
DB_SSL=false

# Render/Neon: use DATABASE_URL in Dashboard instead of DB_*
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# -----------------------------------------------------------------------------
# Render.com deploy - Add in Render Dashboard (not in .env)
# -----------------------------------------------------------------------------
# Required: DATABASE_URL, SECRET_KEY, DEBUG=False, EMAIL_HOST_USER, EMAIL_HOST_PASSWORD
# Optional: DEFAULT_FROM_EMAIL, DJANGO_SUPERUSER_EMAIL, DJANGO_SUPERUSER_USERNAME, DJANGO_SUPERUSER_PASSWORD
# Custom domain: ALLOWED_HOSTS, CSRF_TRUSTED_ORIGINS, SITE_URL
